// lib/settings.mo — domain logic for global settings (xClientId)
module {
  // No stateful helpers needed — settings state lives directly in main.mo
  // as a var xClientId : ?Text, manipulated directly in the mixin.
};
